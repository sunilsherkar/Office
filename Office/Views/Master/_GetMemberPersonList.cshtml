@model IEnumerable<office.Models.MemberPersonList>

@{
    Layout = null;
}

<div class="row">
    <div class="col-lg-12">
        <div class="table-responsive">
            <table class="table">

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="hidden">@item.MemberPersonDetailId</td>
                            <td>@item.PersonName</td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
</div>
